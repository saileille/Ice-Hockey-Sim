// Structs that follow strictly the format defined in the database.
// The convention is that field names are written exactly like they are in the database as well.
// Types can be other structs if the field itself contains JSON.
mod competition;
mod country;
mod game;
mod person;
mod season;
mod team;